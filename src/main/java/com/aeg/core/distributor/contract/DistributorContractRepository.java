package com.aeg.core.distributor.contract;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DistributorContractRepository extends JpaRepository<DistributorContract, Long> {
}
